using System;
using Linq;

public class Users{
  
  public List<string> username,password;
  
  public static void add(string user, pass){
    username.Add(user);
    password.Add(pass)
  }
  public static void main(string[] args){
    
    check();
      if(check == true){
        index.html
      }
    }
    public static bool check(string user, pass){
      if(username != null && password
 != null ){
   if(username.FindIndexOf(user.toString) && password.FindIndexOf(pass.toString)){
     return true;
   }else{
     return false;
   }
 }
    }
  }
}